# React Native's Metro bundler cannot follow pnpm's symlinked store — the
# haste map and native autolinking both assume a flat node_modules. Without
# this the app fails to bundle with "Unable to resolve module" on packages
# that are plainly installed.
node-linker=hoisted
